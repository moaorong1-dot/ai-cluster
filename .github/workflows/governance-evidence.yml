name: Server-side governance evidence

on:
  pull_request_target:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  server-side-provenance:
    name: server-side provenance
    permissions:
      contents: read
      pull-requests: read
    uses: moaorong1-dot/codex-governance-workflows/.github/workflows/attest-governance-evidence.yml@114115468e1e748a33408e4736a485e3d26b40ef
